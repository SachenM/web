@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Money Spent</h1>
    <p class="lead"> Record Your Expenses
    </p>
</div>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-12">
            <h2>@Html.ActionLink("Entry", "Entry", "Home", new { area = "" }, new { @class = "btn btn-primary btn-lg" })</h2>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-lg-12">
            <h2>@Html.ActionLink("Transfer", "Transfer", "Home", new { area = "" }, new { @class = "btn btn-primary btn-lg" })</h2>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-lg-12 align-self-center">
            <h2>@Html.ActionLink("Service", "Service", "Home", new { area = "" }, new { @class = "btn btn-primary btn-lg" })</h2>
        </div>
    </div>
</div>
